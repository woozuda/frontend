import { FloatingActionButton } from "./Button";

export { FloatingActionButton } from "./Button";

const FloatingAction = Object.assign(
  {},
  {
    Button: FloatingActionButton,
  }
);

export default FloatingAction;
